using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace ElevatorTest
{
    class People
    {
        public int id;

        public int currentFloor;
        public int goFloor;

        public People(int maxFloor)
        {
            //Random ro = new Random(10000);
            //currentFloor = ro.Next(1, maxFloor);
            //goFloor = ro.Next(1, maxFloor);
            //while (goFloor == currentFloor)
            //{
            //    goFloor = ro.Next(1, maxFloor);
            //}
        }
    }
}
